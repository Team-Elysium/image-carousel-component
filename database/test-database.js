// Essential tests:

// db.ListingImages model should be capable of saving to the database
// db.ListingImages model should be capable of reading from the database
// db.ListingImages.findOne({id: id}) where should resolve to an object with map and floorPlan properties when id < 100